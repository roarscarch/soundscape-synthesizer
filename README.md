# Soundscape Synthesizer

Generates ambient audio from a text seed.

## Usage
```
pip install -r requirements.txt
python src/main.py --seed "rain forest"
```

## How it works
Seeded PRNG + wave function collapse on audio grains. Multiple biomes supported. Can export to WAV.
